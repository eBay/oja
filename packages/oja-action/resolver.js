'use strict';

const {
    resolveAllUniqueActions,
    resolveAllActions,
    resolveFirstAction,
    resolve,
    resetCache,
    moduleRoot,
    resolveActionLocation
} = require('./lib/actions');

module.exports = {
    resolveAllUniqueActions,
    resolveAllActions,
    resolveFirstAction,
    resolve,
    resetCache,
    moduleRoot,
    resolveActionLocation
};
